{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd ../shared-types && npm run build && cd ../launcher && npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
