{
  "name": "@ttr/app-services",
  "version": "0.0.0",
  "description": "Stream 3: Auth0 + Unifold (PaymentGateway/StubGateway) + REST API (§10.4, §10.5).",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "node --experimental-strip-types --watch src/server.ts",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@ttr/shared-types": "*",
    "@ttr/chain-services": "*",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.0.0"
  }
}
