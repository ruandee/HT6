{
  "name": "@ttr/app-services",
  "version": "0.0.0",
  "description": "Stream 3: Auth0 + Unifold (PaymentGateway/StubGateway) + REST API (§10.4, §10.5).",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "_comment_dev": "Runs from the build rather than the sources. `node --experimental-strip-types src/server.ts` cannot resolve this package's './config.js'-style specifiers back to .ts and dies with ERR_MODULE_NOT_FOUND, so the previous watch-mode dev script did not start at all on Node 22+. Building first costs ~3s and works everywhere; use `dev:watch` if you want reload while editing.",
    "dev": "npm run build && node dist/server.js",
    "dev:watch": "node --watch dist/server.js",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@ttr/shared-types": "*",
    "@ttr/chain-services": "*",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.0.0"
  }
}
